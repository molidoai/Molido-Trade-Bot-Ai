# Trading Engine dependencies
# Shared packages are installed in editable mode from monorepo root

pydantic==2.10.4
pydantic-settings==2.7.0
structlog==24.4.0
mt5linux
# For the optional regime-model volatility signal (molido_regime.ml_engine).
# Pinned to match scripts/train_regime_model.py's training environment --
# unpickling a model trained on a different sklearn version can warn/break.
numpy==2.1.3
scikit-learn==1.5.2
# MetaTrader5==5.0.45   # Uncomment only on Windows / Wine environment with MT5 terminal
